{
  "private": true,
  "license": "MPL-2.0",
  "scripts": {
    "build": "npm run build --workspaces",
    "start": "npm run start -w @web-speed-hackathon-2021/server",
    "deploy": "npm run build && gcloud app deploy --project=pvcresin-wsh-2021"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "engines": {
    "node": "16.x.x"
  }
}
