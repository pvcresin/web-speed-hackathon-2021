{
  "private": true,
  "license": "MPL-2.0",
  "scripts": {
    "gcp-build": "yarn build",
    "build": "yarn workspaces run build",
    "start": "yarn workspace @web-speed-hackathon-2021/server start",
    "deploy": "gcloud app deploy --project=pvcresin-wsh-2021"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "engines": {
    "node": "16.x.x"
  }
}
